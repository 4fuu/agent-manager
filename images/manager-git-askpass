#!/bin/sh
# Git requests credentials on a private pipe; no token in argv, URL, or Git config.
case "$1" in
  *Username*) printf '%s\n' x-access-token ;;
  *Password*) cat /run/manager/github-token ;;
  *) exit 1 ;;
esac
